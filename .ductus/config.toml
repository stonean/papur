[migrations]
last_applied = "generator-primitives"

[project]
name = "papur"
description = "A markdown-flavored markup language that compiles to semantic, accessible HTML, CSS, and beyond."
languages = ["Rust"]

[rules]
surfaces = ["backend"]

[paths]
specs-root = "specs"

[review]
tech-stack-verified = true

# ductus (host)
[host]
project = "papur"
