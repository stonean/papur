[migrations]
last_applied = "runtime-store-path"

[project]
name = "papur"
description = "A markdown-flavored markup language that compiles to semantic, accessible HTML, CSS, and beyond."
languages = ["Rust"]

[rules]
surfaces = ["backend"]

[paths]
specs-root = "specs"

[review]
tech-stack-verified = true

# ductus (host)
[host]
project = "papur"
